import navbar from "../navbar.js";
import sidebar from "../sidebar.js";

const config = {
  hostname: "localhost",
  author: {
    name: "前端攻城喵",
    url: "",
  },
  navbar,
  sidebar,
};

export default config;
